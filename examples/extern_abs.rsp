; Call libc abs via (extern "C" …).
;;! stdout: ok: ffi
;;! exit: 0

(extern "C" abs [x: i32] -> i32)

(defn main [] -> i32
  (if (= (abs (- 42)) 42)
    (do (println "ok: ffi") 0)
    (do (println "FAIL: ffi") 1)))
